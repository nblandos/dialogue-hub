services:
  backend:
    build: ./backend
    image: team26.azurecr.io/backend:latest
    ports:
      - "5001:5001"
    volumes:
      - ./backend:/app
      - sqlite_data:/app/instance
      - /app/__pycache__
    env_file:
      - ./backend/.env
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - FLASK_DEBUG=1

  frontend:
    build: ./frontend
    image: team26.azurecr.io/frontend:latest
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules/
    environment:
      - VITE_API_URL=http://backend:5001
      - NODE_ENV=development
    depends_on:
      - backend

volumes:
  sqlite_data:
