FROM python:3.12-slim-bookworm

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN mkdir -p instance && chmod 755 instance

EXPOSE 5001

CMD ["gunicorn", "-b", "0.0.0.0:5001", "app:app", "--workers", "4", "--timeout", "120"]